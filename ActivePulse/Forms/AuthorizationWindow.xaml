<Window x:Class="ActivePulse.Forms.AuthorizationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ActivePulse.Forms"
        mc:Ignorable="d"
        Title="ActivePulse - Авторизация" 
        Height="600" 
        Width="850"
        WindowStartupLocation="CenterScreen"
        Background="#FF424242">

    <Window.Resources>
        <!-- Стиль для текстовых полей с закругленными углами -->
        <Style TargetType="TextBox" x:Key="AuthTextBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Background" Value="#FFEEEEEE"/>
            <Setter Property="BorderBrush" Value="#FFD4A017"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="5"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost"
                                        Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для поля пароля с закругленными углами -->
        <Style TargetType="PasswordBox" x:Key="AuthPasswordBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Background" Value="#FFEEEEEE"/>
            <Setter Property="BorderBrush" Value="#FFD4A017"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border CornerRadius="5"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для кнопки Войти -->
        <Style TargetType="Button" x:Key="AuthButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="5"
                                Background="{TemplateBinding Background}"
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="10,0"/>
        </Style>

        <!-- Обновленный стиль для текстовой кнопки (без свечения) -->
        <Style TargetType="Button" x:Key="TextButton">
            <Setter Property="Foreground" Value="#FFD4A017"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="5,0,0,0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <TextBlock Text="{TemplateBinding Content}" 
                                   Foreground="{TemplateBinding Foreground}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="#FFF0A500"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Основной контейнер с тенью -->
        <Border Background="#FF555555"
                CornerRadius="10"
                Width="350"
                Height="450"
                VerticalAlignment="Center"
                HorizontalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" 
                                ShadowDepth="5" 
                                Opacity="0.2"/>
            </Border.Effect>

            <StackPanel VerticalAlignment="Center" 
                       HorizontalAlignment="Center"
                       Width="280">

                <!-- Заголовок Welcome -->
                <TextBlock Text="Welcome to ActivePulse"
                           FontSize="22"
                           FontWeight="Bold"
                           Foreground="#FFD4A017"
                           TextAlignment="Center"
                           Margin="0,0,0,20"/>

                
                <TextBlock Text="Войдите в аккаунт"
                           FontSize="16"
                           Foreground="#FFCCCCCC"
                           TextAlignment="Center"
                           Margin="0,0,0,30"/>
                <TextBox Style="{StaticResource AuthTextBox}"
                         x:Name="LoginTextBox" Height="40"/>

                <!-- Поле для логина -->

                <!-- Поле для пароля -->
                <PasswordBox Style="{StaticResource AuthPasswordBox}"
                            x:Name="PasswordTextBox" Height="40"/>

                <!-- Кнопка Войти -->
                <Button x:Name="LoginButton"
                        Content="Войти"
                        Style="{StaticResource AuthButton}"
                        Background="#FFD4A017"
                        Margin="0,10,0,20" Click="LoginButton_Click"/>

                <!-- Текст с предложением регистрации -->
                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            Margin="0,0,0,10">
                    <TextBlock Text="Еще нет аккаунта? "
                               FontSize="12"
                               Foreground="#FFCCCCCC"/>
                    <Button Content="Зарегистрируйтесь!"
                            Style="{StaticResource TextButton}" Click="RegistrationButton_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>