<Window x:Class="ActivePulse.Forms.FavouritesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ActivePulse.Forms"
        mc:Ignorable="d"
        Title="Избранное" 
        Height="500" 
        Width="800"
        WindowStartupLocation="CenterOwner"
        Background="#FF424242">

    <Window.Resources>
        <Style TargetType="Button" x:Key="ActionButton" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#FF555555"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FFD4A017"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="150"/>
        </Style>

        <Style TargetType="Button" x:Key="RemoveButton" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Content" Value="Удалить"/>
            <Setter Property="Background" Value="#FF6D1F1F"/>
            <Setter Property="BorderBrush" Value="#FF9E2D2D"/>
        </Style>
    </Window.Resources>

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="FavouritesItemsControl">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="10" Padding="10" Background="#FF555555" 
                                CornerRadius="5" Width="200" Height="280">
                            <StackPanel>
                                <Image Source="{Binding ImagePath}" Height="100" Stretch="Uniform"/>
                                <TextBlock Text="{Binding ProductName}" Margin="0,5" 
                                           TextWrapping="Wrap" Foreground="White"/>
                                <TextBlock Text="{Binding Price, StringFormat='{}{0} руб.'}" 
                                           Foreground="#FFD4A017"/>

                                <Button Content="В корзину" Style="{StaticResource ActionButton}"
                                        Click="AddToCart_Click" Tag="{Binding ProductId}"/>

                                <Button Style="{StaticResource RemoveButton}"
                                        Click="RemoveFromFavourites_Click" 
                                        Tag="{Binding ProductId}"
                                        Margin="0,5,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>